servlet是浏览器和服务器之间的中间层。
可以使用servlet收集网页表单的用户输入，呈现来自数据库的记录，还可以动态创建网页。

servlet用来：
  读取浏览器发送的显式数据。
  读取浏览器发送的隐式HTTP请求数据，包括cookies，媒体类型和浏览器能解压的压缩格式。
  处理数据并生成结果。这个过程需要访问数据库
  发送显式的数据到浏览器。
  发送隐式的HTTP响应到浏览器。
